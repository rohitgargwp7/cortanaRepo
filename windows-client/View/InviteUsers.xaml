<phone:PhoneApplicationPage 
    x:Class="windows_client.View.InviteUsers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="clr-namespace:windows_client.converters"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="White">

        <Grid.Resources>
            <Converters:ImageConverter x:Key="imgConverter"></Converters:ImageConverter>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="HikeTitle" Grid.Row="0" HorizontalAlignment="Left">
            <Image Source="images\hike_header.png" Margin="0,-7,0,0" Height="62" Width="127"></Image>
        </StackPanel>

        <StackPanel>
            <TextBlock x:Name="hikeLabel" Text="invite" FontSize="45" Height="70" Margin="35,60,0,0" Width="Auto" Foreground="Black"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <ListBox x:Name="inviteListBox" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" FontSize="26" Margin="0,-30,-25,75" Tap="inviteListBox_Tap">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="22,5,5,5">
                        <Image x:Name="avatar_image" Height="65" HorizontalAlignment="Left" VerticalAlignment="Center" 
                               Source="{Binding Msisdn, Converter={StaticResource imgConverter}}"
                               >
                        </Image>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Name}" Width="280" FontSize="38" Height="50" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Black" />
                            <TextBlock Text="{Binding Msisdn}" Width="280" FontSize="25" Height="50" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Black" />
                        </StackPanel>
                        <Button Content="invite" BorderThickness=".3" BorderBrush="DarkBlue" Margin="10,15,0,-5" FontSize="32" Height="30" Grid.Column="1" Grid.ColumnSpan="1" Foreground="White" d:LayoutOverrides="GridBox" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Right">
                            <Button.Background>
                                <ImageBrush ImageSource="Images\ic_bottom_button.png" Stretch="Fill"/>
                            </Button.Background>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>
