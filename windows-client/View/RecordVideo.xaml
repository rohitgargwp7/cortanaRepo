<!-- 
    Copyright (c) 2011 Microsoft Corporation.  All rights reserved.
    Use of this sample source code is subject to the terms of the Microsoft license 
    agreement under which you licensed this sample source code and is provided AS-IS.
    If you did not accept the terms of the license agreement, you are not authorized 
    to use this sample source code.  For the terms of the license, please see the 
    license agreement between you and Microsoft.
    
    To see all Code Samples for Windows Phone, visit http://go.microsoft.com/fwlink/?LinkID=219604
-->
<phone:PhoneApplicationPage 
    x:Class="windows_client.View.RecordVideo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="480"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Landscape" Orientation="LandscapeLeft"
    shell:SystemTray.IsVisible="False" Name="recordVideo">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">

        <Image x:Name="previewImage" Canvas.ZIndex="5"/>
        
        <!--Camera viewfinder >-->
        <Rectangle 
            x:Name="viewfinderRectangle"
            Width="640" 
            Height="480" 
            HorizontalAlignment="Left" 
            Margin="80,0,0,0">
            <Rectangle.Fill>
                <VideoBrush x:Name="videoRecorderBrush" AlignmentX="Left" AlignmentY="Top" Stretch="UniformToFill">
                    <VideoBrush.RelativeTransform>
                        <CompositeTransform x:Name="viewfinderTransform" ScaleX="1"
                                            CenterX="0.5" CenterY="0.5"/>
                    </VideoBrush.RelativeTransform>
                </VideoBrush>
            </Rectangle.Fill>
        </Rectangle>

        <MediaElement 
            x:Name="VideoPlayer" 
            AutoPlay="True" 
            RenderTransformOrigin="0.5, 0.5" 
            VerticalAlignment="Center" 
            HorizontalAlignment="Left" 
            Stretch="Fill" Height="480" Width="640"
            Margin="80,0,0,0">
            <MediaElement.RenderTransform>
                <TransformGroup>
                    <CompositeTransform x:Name="playerTransform" ScaleX="1"
                                            CenterX="0.5" CenterY="0.5" />
                </TransformGroup>
            </MediaElement.RenderTransform>
        </MediaElement>

        <!--Used for debugging >-->
        <StackPanel Orientation="Horizontal" Margin="12" VerticalAlignment="Bottom">
            <TextBlock 
            HorizontalAlignment="Left" 
            VerticalAlignment="Bottom"
            Name="txtDebug" FontWeight="Thin"
            Text="00.00" Foreground="White" Margin="80,0,0,0"
            FontSize="100" />

            <TextBlock 
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Name="txtSize" Foreground="White"
            Margin="90,0,0,15"
            FontSize="24"/>
        </StackPanel>

        <Grid x:Name="SettingsGrid" Canvas.ZIndex="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed">
            <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{StaticResource PhoneBackgroundBrush}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <ListBox FontSize="40" x:Name="resolutionList" HorizontalAlignment="Center" VerticalAlignment="Center" SelectionChanged="resolutionList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Height}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <ListBox FontSize="40" Margin="25,0,0,0" x:Name="cameraList" HorizontalAlignment="Center" VerticalAlignment="Center" SelectionChanged="cameraList_SelectionChanged"/>
            </StackPanel>
        </Grid>
    </Grid>
</phone:PhoneApplicationPage>
