<phone:PhoneApplicationPage 
    x:Class="windows_client.EnterPin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="480"
    shell:SystemTray.IsVisible="False" Name="enterPin"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit">

    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>
    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileTransition Mode="BackwardOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileTransition Mode="ForwardOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>



    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent" Margin="0,32,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Margin="0,0,0,0" Grid.Row="0">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="500" >
                <Image Source="images\hike_logo_small.png" Height="40" Margin="0,15,0,0"></Image>
                <Rectangle Height="1" Margin="2,15,0,0" Fill="#FFD5D5D5"></Rectangle>

                <TextBlock x:Name="txtBlkSMSPin" TextWrapping="Wrap" Text="Thanks! Did you get an SMS pin?" FontSize="24"  TextAlignment="Center" Margin="-11,28,8,0" d:LayoutOverrides="GridBox" RenderTransformOrigin="0.5,0.5" Height="40" VerticalAlignment="Top" />
                <TextBlock x:Name="txtBlkEnterPin" TextWrapping="Wrap" Text="Enter it here" FontSize="24" TextAlignment="Center" Margin="0,0,0,0" d:LayoutOverrides="GridBox" RenderTransformOrigin="0.497,2.059" Height="40" VerticalAlignment="Top" />

                <Border BorderThickness="2" Width="310" Height="56" Margin="5,10,0,0" BorderBrush="Black">
                    <StackPanel Orientation="Horizontal" Height="50"   Width="320" Margin="0,0,0,0" Background="White">
                        <Image Source="images\sms_pin.png" Margin="15,10,0,10" Height="20" />
                        <toolkit:PhoneTextBox x:Name="txtBxEnterPin" LostFocus="txtBxEnterPin_LostFocus" GotFocus="txtBxEnterPin_GotFocus" Foreground="Black" BorderThickness="0" InputScope="Number" Margin="0,-10,0,0" Width="250" Height="70" TextChanged="txtBxEnterPin_TextChanged" />
                    </StackPanel>
                </Border>
                <TextBlock Height="25" HorizontalAlignment="Center" 
                           VerticalAlignment="Center" Margin="0,3,0,0" 
                           Name="pinErrorTxt" Text="Invalid PIN. Try again !!" 
                           Opacity="0" FontSize="18" Foreground="Red"/>

                
                <StackPanel Orientation="Horizontal" Height="50" Margin="0,5,0,0" >
                    <TextBlock Text="Entered a wrong number?" FontSize="20" Margin="90,10,-10,5"></TextBlock>
                    <Button x:Name="btnWrongMsisdn" Content="Tap Here" Foreground="#399cdf" FontSize="20" Background="Transparent" BorderThickness="0" Height="65" Margin="0,-7,0,-8" Click="btnWrongMsisdn_Click"></Button>
                </StackPanel>
                
                <toolkit:PerformanceProgressBar x:Name="progressBar" Background="Transparent" 
                                                IsIndeterminate="True" VerticalAlignment="Center" VerticalContentAlignment="Center" 
                                                FontSize="24" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" 
                                                IsEnabled="False" IsTabStop="True" Opacity="0" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</phone:PhoneApplicationPage>
