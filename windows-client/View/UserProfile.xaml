<phone:PhoneApplicationPage
    x:Class="windows_client.View.UserProfile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="315" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image Name="imgBackgrund" Opacity="0.6"  Source="images/trial.jpg" Grid.Row="0" Grid.RowSpan="3" Stretch="UniformToFill" Margin="-4,5,4,-5" />

            <Grid Grid.Row="0" Margin="24,22,24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtProfileHeader" Text="{Binding Path= LocalizedResources.OthersProfile_Txt, Source={StaticResource LocalizedStrings}}"  Grid.Column="0" HorizontalAlignment="Left" FontSize="22" FontFamily="Segoe WP"  />
                <!--<Grid x:Name="gridOnlineBusyIcon" Grid.Column="1" HorizontalAlignment="Right" >
                    <Ellipse Fill="#3D3b36" Width="18" Height="18" HorizontalAlignment="Center" />
                    <Ellipse  Fill="#4CB700" Width="10" Height="10"  HorizontalAlignment="Center" />
                </Grid>-->
            </Grid>
            <Grid Grid.Row="1" Margin="24,55,24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Name="avatarImage" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Column="0" MaxHeight="96" MaxWidth="96"   />
                <Image Name="changePic" Visibility="Collapsed" Grid.Column="0"  Width="31" Height="28" HorizontalAlignment="Right" VerticalAlignment="Bottom" Source="images\change_pic.png" />
            </Grid>
            <StackPanel Grid.Row="2" Margin="24,24,24,0" Orientation="Vertical" >
                <TextBlock Name="txtUserName" HorizontalAlignment="Left" FontSize="30" FontFamily="Segoe WP"/>
                <TextBlock Name="txtOnHikeSmsTime" Text="{Binding Path= LocalizedResources.OnHIkeSince_Txt, Source={StaticResource LocalizedStrings}}"  HorizontalAlignment="Left" FontSize="15"  FontFamily="Segoe WP"   />
            </StackPanel>
        </Grid>
        <Grid Name="gridSmsUser" Grid.Row="1"  >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image x:Name="imgInviteLock" Grid.Row="0" Source="images/tick.png" HorizontalAlignment="Center" MaxHeight="30" MaxWidth="30" Margin="0,30,0,0"></Image>
            <TextBlock Grid.Row="1" FontSize="18" TextWrapping="Wrap" Margin="0,15,0,0" HorizontalAlignment="Center" Foreground="#5f5f5f" TextAlignment="Center" >
                <TextBlock.Inlines>
                    <Run x:Name="txtSmsUserNameBlk1" FontFamily="Segoe WP" FontWeight="SemiBold"/>
                    <Run x:Name="txtSmsUserNameBlk2" Text="{Binding Path= LocalizedResources.InviteOnHike_Txt, Source={StaticResource LocalizedStrings}}" />
                    <LineBreak/>
                    <Run x:Name="txtSmsUserNameBlk3" Text="{Binding Path= LocalizedResources.InviteOnHikeUpgrade_Txt, Source={StaticResource LocalizedStrings}}"/>
                </TextBlock.Inlines>
            </TextBlock>
            <Button Name="btnInvite" Grid.Row="2" Content="{Binding Path= LocalizedResources.InviteOnHikeBtn_Txt, Source={StaticResource LocalizedStrings}}"  FontSize="15" Width="150" Height="60" Background="#2e91d3" BorderThickness="0" Foreground="White"  Margin="0,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" Tap="Invite_Tap"/>
        </Grid>
        <Grid Name="gridHikeUser" Grid.Row="1" >
            <phone:LongListSelector x:Name="statusLLS" Grid.Row="1" IsGroupingEnabled="False" LayoutMode="List">
                <phone:LongListSelector.ItemTemplate>
                    <DataTemplate>
                    </DataTemplate>
                </phone:LongListSelector.ItemTemplate>
            </phone:LongListSelector>
        </Grid>
    </Grid>
</phone:PhoneApplicationPage>