<phone:PhoneApplicationPage 
    x:Class="windows_client.View.ShareLocation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="clr-namespace:Microsoft.Phone.Maps.Controls;assembly=Microsoft.Phone.Maps"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True" Name="shareLocation">

    <shell:SystemTray.ProgressIndicator>
        <shell:ProgressIndicator x:Name="shellProgress" IsIndeterminate="true" IsVisible="True" />
    </shell:SystemTray.ProgressIndicator>

    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="RouteListTemplate">
            <StackPanel Margin="5,5,0,0">
                <TextBlock Text="{Binding title}" Style="{StaticResource PhoneTextTitle3Style}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding vicinity}" Style="{StaticResource PhoneTextSubtleStyle}" TextWrapping="Wrap"/>
            </StackPanel>
        </DataTemplate>
    </phone:PhoneApplicationPage.Resources>
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <maps:Map x:Name="MyMap" ZoomLevelChanged="ZoomLevelChanged" Tap="map_Tap"/>

        <phone:LongListSelector Grid.Row="1" MaxHeight="350" Visibility="Collapsed" x:Name="PlacesList" Background="Transparent" ItemTemplate="{StaticResource RouteListTemplate}" LayoutMode="List" 
            IsGroupingEnabled="False" SelectionChanged="PlacesList_SelectionChanged"/>
        <ScrollViewer Grid.Row="1" x:Name="stackPanelDetail" Visibility="Collapsed">
            <StackPanel Orientation="Vertical" Tap="map_Tap" MaxHeight="350" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <TextBlock Text="{Binding title}" Style="{StaticResource PhoneTextTitle3Style}" TextWrapping="Wrap" HorizontalAlignment="Left"/>
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel  Orientation="Vertical"  MaxWidth="200">
                        <TextBlock Text="{Binding vicinity}" TextWrapping="Wrap" />
                        <TextBlock Text="{Binding category.title, StringFormat='Category: {0}'}" TextWrapping="Wrap" />
                        <TextBlock Text="{Binding averageRating, StringFormat='Rating: {0:0.0}'}" />
                        <TextBlock Text="{Binding distance, StringFormat='Distance: {0}'}" />
                    </StackPanel>
                    <Image Grid.Column="1" x:Name="image" Margin="10,0,0,0" MaxWidth="320" MaxHeight="180" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        

        <!--Panel of buttons to control map cartography mode-->
        <StackPanel x:Name="ModePanel" Orientation="Vertical" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="281" Margin="0,0,0,12">
            <Button x:Name="RoadButton" Content="Road" IsEnabled="False" Click="CartographicModeButton_Click"/>
            <Button x:Name="AerialButton" Content="Aerial" Click="CartographicModeButton_Click"/>
            <Button x:Name="HybridButton" Content="Hybrid" Click="CartographicModeButton_Click"/>
            <Button x:Name="TerrainButton" Content="Terrain" Click="CartographicModeButton_Click"/>
        </StackPanel>

        <!--Sliders to control map pitch and heading.-->
        <Slider x:Name="PitchSlider" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Bottom" Height="211" Width="45" Margin="0,0,12,82" Minimum="0" Maximum="70" Value="0" ValueChanged="PitchValueChanged"/>
        <Slider x:Name="HeadingSlider" VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="80" Width="250" Margin="0,0,0,-20" Maximum="540" Minimum="180" Value="360" ValueChanged="HeadingValueChanged"/>
    </Grid>
</phone:PhoneApplicationPage>
