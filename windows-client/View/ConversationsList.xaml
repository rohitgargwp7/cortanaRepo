<phone:PhoneApplicationPage  
    x:Class="windows_client.View.ConversationsList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:local="clr-namespace:windows_client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    toolkit:TiltEffect.IsTiltEnabled="True"
    shell:SystemTray.IsVisible="True" Name="convListPagePivot">

    <shell:SystemTray.ProgressIndicator>
        <shell:ProgressIndicator x:Name="shellProgress" IsIndeterminate="true" IsVisible="false" />
    </shell:SystemTray.ProgressIndicator>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <!--Pivot Control-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <StackPanel Orientation="Vertical" Grid.Row="0" HorizontalAlignment="Left" Margin="20,-10,0, 0">
                <Image x:Name="icon" Source="images\hike_header.png" Height="40" Margin="4,25,0,0"/>
            </StackPanel>

        </StackPanel>
        <controls:Pivot Grid.Row="1"  Margin="0,-10,0,0" SelectionChanged="Pivot_SelectionChanged">
            <!--Pivot item one-->
            <controls:PivotItem Header="chats">
                <Grid x:Name="ConversationList">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!--ContentPanel - place additional content here-->

                    <Image Grid.Column="0" Grid.Row="0" x:Name="emptyScreenImage" Width="135" Height="152" Opacity="0" VerticalAlignment="Center" Margin="0,0,0,100">
                        <!--<Image.Source>
                            <BitmapImage UriSource="images\empty_screen_logo_black.png" CreateOptions="BackgroundCreation"/>
                        </Image.Source>-->
                    </Image>

                    <Image Grid.Column="0" Grid.Row="0" x:Name="emptyScreenTip" Width="295" Height="76" Opacity="0" VerticalAlignment="Bottom" Margin="0,0,4,10">
                        <!--<Image.Source>
                            <BitmapImage UriSource="images\empty_screen_tip_black.png" CreateOptions="BackgroundCreation"/>
                        </Image.Source>-->
                    </Image>
                    <ScrollViewer x:Name="convScroller" Height="530" Margin="0, -10,0,0">
                        <ListBox ScrollViewer.VerticalScrollBarVisibility="Disabled" Grid.Row="0" Grid.Column="0" x:Name="myListBox" Grid.ColumnSpan="1" FontSize="26" Tap="btnGetSelected_Tap" Margin="-9,0,-25,5" Grid.RowSpan="1" VirtualizingStackPanel.VirtualizationMode="Recycling">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal" Margin="22,0,5,6">
                                            <Image x:Name="avatar_image" Height="69" HorizontalAlignment="Left" VerticalAlignment="Center" Source="{Binding AvatarImage}"/>

                                            <StackPanel Orientation="Vertical">
                                                <StackPanel Orientation="Horizontal" Margin="20, 0, 0, 0">
                                                    <TextBlock Text="{Binding NameToShow}" Width="280" FontSize="39" FontFamily="Segoe WP SemiLight" FontStyle="Normal" FontWeight="ExtraLight" Height="53" Grid.Column="0" Grid.ColumnSpan="2"/>
                                                    <TextBlock Text="{Binding FormattedTimeStamp}" Margin="14,15,10,-5" FontSize="20" Height="30" Grid.Column="1" Grid.ColumnSpan="1" Foreground="Gray"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="10, 0, 0, 0" MaxHeight="35" VerticalAlignment="Top">
                                                    <Image Height="13" Margin="10,0,0,0" Visibility="{Binding SDRStatusImageVisible}" Source="{Binding SDRStatusImage}" VerticalAlignment="Center"></Image>
                                                    <RichTextBox Tag="{Binding LastMessage}" TextWrapping="Wrap" MaxWidth="325" FontSize="21" Foreground="{Binding LastMessageColor}" 
                                                             Loaded="RichTextBox_Loaded" Margin="0,-2,0,5" Height="35" VerticalAlignment="Center" />
                                                    <!--<local:LinkifiedTextBox Text="{Binding LastMessage}" FontSize="21" ForegroundColor="{Binding LastMessageColor}" MaxHeight="35" Grid.Column="1" Grid.ColumnSpan="3" VerticalAlignment="Top"/>-->
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                        <toolkit:ContextMenuService.ContextMenu>
                                            <toolkit:ContextMenu Name="ctxMenu" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <toolkit:MenuItem Header="Delete" Tap="MenuItem_Tap_Delete"/>
                                            </toolkit:ContextMenu>
                                        </toolkit:ContextMenuService.ContextMenu>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <!--<toolkit:PerformanceProgressBar x:Name="progressBar" Margin="0,0,0,10" Background="Transparent" IsIndeterminate="True" VerticalAlignment="Bottom" VerticalContentAlignment="Center" FontSize="24" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" IsEnabled="False" IsTabStop="True" Opacity="0" />-->
                    </ScrollViewer>
                </Grid>
            </controls:PivotItem>

            <controls:PivotItem Header="profile">
                <Grid x:Name="ProfileGrid">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image x:Name="avatarImage" Grid.Column="0" MaxHeight="83" MaxWidth="83" Margin="15,0,5,0" Tap="onProfilePicButtonTap" Source="images\default_user.png" />
                        <Image Grid.Column="0"  Width="31" Height="28" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,0" Source="images\change_pic.png" />

                        <StackPanel Grid.Column="1" Name="accountStackPanel" Orientation="Vertical" Tap="EditProfile_Tap" Margin="20, 0,0,0">
                            <TextBlock x:Name="accountName" FontSize="42" FontFamily="Segoe WP SemiLight" Text="UNKNOWN"/>
                            <TextBlock x:Name="editProfileTextBlck" Text="Edit Profile"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0, 15, 0,10" Orientation="Vertical" Grid.Row="1">
                        <StackPanel x:Name="freeSMSPanel" Orientation="Horizontal"  Tap="FreeSMS_Tap" Margin="15,15,0,5">
                            <Image Name ="freeSmsImage" VerticalAlignment="Top" Margin="0,10,0,0" Source="images\free_sms.png" Height="35">
                            </Image>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="Free SMS" FontFamily="Segoe WP SemiLight" Width="380" Margin="15,0,0,0" FontSize="39"></TextBlock>
                                <TextBlock x:Name="creditsTxtBlck" FontSize="18" Margin="15,0,0,0"></TextBlock>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel  Orientation="Horizontal" MaxHeight="55" Tap="Invite_Tap" Margin="15,15,0,5">
                            <Image Name ="invite" VerticalAlignment="Center" Height="35">
                            </Image>
                            <TextBlock Text="Tell a friend" FontFamily="Segoe WP SemiLight" Width="380" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="39"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" MaxHeight="55" Margin="15,15,0,5" Tap="Privacy_Tap">
                            <Image Name="privacyImage" VerticalAlignment="Center" Source="images\privacy.png" Width="26" Height="35"></Image>
                            <TextBlock Text="Privacy" FontFamily="Segoe WP SemiLight" Width="380" VerticalAlignment="Center" Margin="25,0,0,0" FontSize="39"></TextBlock>
                        </StackPanel>
                        <StackPanel  Orientation="Horizontal" MaxHeight="55" Tap="Notifications_Tap" Margin="15,15,0,5">
                            <Image Name="settingsImage" VerticalAlignment="Center" Source="images\settings.png" Height="34" Width="34">
                            </Image>
                            <TextBlock Text="Settings" FontSize="39" FontFamily="Segoe WP SemiLight" Width="380" VerticalAlignment="Center" Margin="15,0,0,0"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" MaxHeight="55" Margin="15,15,0,5" Tap="Help_Tap">
                            <Image Name="helpImage" VerticalAlignment="Center" Source="images\help.png" Height="35"></Image>
                            <TextBlock Text="Help" FontFamily="Segoe WP SemiLight" Width="380" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="39"></TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </controls:PivotItem>
            
            <controls:PivotItem Header="favourites">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <ScrollViewer Grid.Row="0" >
                        <Grid Margin="16,0,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>

                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" FontSize="24" Foreground="#3c3c3c" Text="Your favourites"/>

                            <toolkit:LongListSelector x:Name="pendingRequests" ScrollViewer.VerticalScrollBarVisibility="Disabled" Grid.Row="1" IsFlatList="True">
                                <toolkit:LongListSelector.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"></RowDefinition>
                                                <RowDefinition Height="Auto"></RowDefinition>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding AvatarImage}" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Top"></Image>
                                            <TextBlock TextWrapping="Wrap" Grid.Column="1" Grid.Row="0">
                                                <TextBlock.Inlines>
                                                    <Run FontWeight="Bold" Text="{Binding ContactName}"/>
                                                    <Run FontWeight="Light" Text=" added you as favourite. Would you like to do the same?"/>
                                                </TextBlock.Inlines>
                                            </TextBlock>
                                            <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1">
                                                <Button Background="Blue" Content="Yes"></Button>
                                                <Button Background="LightGray" Content="Not Now"></Button>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </toolkit:LongListSelector.ItemTemplate>
                            </toolkit:LongListSelector>

                            <toolkit:LongListSelector x:Name="favourites" ScrollViewer.VerticalScrollBarVisibility="Disabled" Grid.Row="2" IsFlatList="True">
                                <toolkit:LongListSelector.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="{Binding AvatarImage}" Height="50" Margin="0,10,0,0"></Image>
                                            <TextBlock Text="{Binding ContactName}" FontSize="22" VerticalAlignment="Center" Foreground="#2d2d2d"></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </toolkit:LongListSelector.ItemTemplate>
                            </toolkit:LongListSelector>

                            <Rectangle Height="1" Fill="#080808" Grid.Row="3"/>
                            
                            <TextBlock Text="Friends on hike" Grid.Row="4" Foreground="#3c3c3c" FontSize="24"></TextBlock>
                            <ListBox x:Name="hikeFriends" ScrollViewer.VerticalScrollBarVisibility="Disabled" Grid.Row="5">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="{Binding AvatarImage}" Height="50" Margin="0,10,0,0"></Image>
                                            <TextBlock Text="{Binding Name}" FontSize="22" Foreground="#2d2d2d" VerticalAlignment="Center" Margin="10,0,0,0" Width="320"></TextBlock>
                                            <!--<Button Content="+"  FontSize="28" Height="70"></Button>-->
                                            <Border Height="40" Width="40" VerticalAlignment="Center" Margin="0,8,0,0" BorderThickness="2" BorderBrush="Black">
                                                <TextBlock Text="+" Margin="0,0,0,3"  FontSize="28" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

</phone:PhoneApplicationPage>
