<phone:PhoneApplicationPage 
    x:Class="windows_client.View.ConversationsList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:Converters="clr-namespace:windows_client.converters"
        xmlns:GkJumpList="clr-namespace:GK.Phone;assembly=Gk.Phone"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False" Name="convListPagePivot">

    <!--<toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:SlideTransition Mode="SlideRightFadeIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:SlideTransition Mode="SlideLeftFadeIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>
    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:SlideTransition Mode="SlideRightFadeOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:SlideTransition Mode="SlideLeftFadeOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>-->

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="White">
        <!--Pivot Control-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" HorizontalAlignment="Left" Margin="20,20,0, 0">
            <Image x:Name="icon" Source="images\hike_header.png" Height="40"/>
        </StackPanel>

        <controls:Pivot Grid.Row="1" Foreground="Black" Margin="0,-20,0,0" SelectionChanged="Pivot_SelectionChanged">
            <!--Pivot item one-->
            <controls:PivotItem Header="conversations">
                <Grid x:Name="ConversationList">
                    <Grid.Resources>
                        <Converters:ImageConverter x:Key="imgConverter"></Converters:ImageConverter>
                    </Grid.Resources>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="437"/>
                        <ColumnDefinition Width="57*"/>
                    </Grid.ColumnDefinitions>

                    <!--ContentPanel - place additional content here-->
                    <ListBox x:Name="myListBox" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" FontSize="26" Tap="btnGetSelected_Click" Margin="0,-30,-25,75" >
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal" Margin="22,5,5,5">
                                        <Image x:Name="avatar_image" Height="65" HorizontalAlignment="Left" VerticalAlignment="Center" 
                               Source="{Binding Msisdn, Converter={StaticResource imgConverter}}">
                                        </Image>
                                        <StackPanel Orientation="Vertical">
                                            <StackPanel Orientation="Horizontal" Margin="25 0 0 0">
                                                <TextBlock Text="{Binding ContactName}" Width="280" FontSize="38" Height="50" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Black" />
                                                <TextBlock Text="{Binding TimeStamp}" Margin="10,15,10,-5" FontSize="20" Height="30" Grid.Column="1" Grid.ColumnSpan="1" Foreground="Black"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding LastMessage}" FontSize="20" Margin="30 0 0 2" Height="35"  Grid.Column="1" Grid.ColumnSpan="3" VerticalAlignment="Top" Foreground="Gray"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <!--<Rectangle Fill="LightGray" Height="1" HorizontalAlignment="Stretch" Width="700"/>-->
                                    <toolkit:ContextMenuService.ContextMenu>
                                        <toolkit:ContextMenu Name="ctxMenu" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <toolkit:MenuItem Header="Delete"  Click="MenuItem_Click_Delete"/>
                                            <toolkit:MenuItem Header="Add Shortcut"/>
                                        </toolkit:ContextMenu>
                                    </toolkit:ContextMenuService.ContextMenu>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </controls:PivotItem>

            <!--Pivot item two-->
            <!--<controls:PivotItem Header="new chat">
                <Grid x:Name="newChat" Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="620"/>
                        <RowDefinition Height="0*"/>
                    </Grid.RowDefinitions>
                    <Grid.Resources>
                        <Converters:ImageConverter x:Key="imgConverter"></Converters:ImageConverter>
                        <Converters:OnHikeIndicator x:Key="onHikeConverter"></Converters:OnHikeIndicator>
                    </Grid.Resources>

                    <StackPanel Background="White">
                        <StackPanel x:Name="HikeTitle" Orientation="Vertical" Margin="0,0,0,0" Height="Auto">
                            <Image Source="images\hike_logo_small.png" Margin="25,20,0,0" HorizontalAlignment="Left" Height="34" Width="127"></Image>
                            <TextBlock Text="new chat" FontFamily="Calibri" FontSize="55" FontStretch="Expanded"  Margin="23,0,0,0" Foreground="Black" Height="56"/>
                        </StackPanel>
                    </StackPanel>

                    <Grid x:Name="ContentPanel" Grid.Row="1" Margin="0,0,0,0" Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.206*"/>
                            <ColumnDefinition Width="0.794*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <toolkit:PhoneTextBox x:Name="enterNameTxt" GotFocus="enterNameTxt_GotFocus" Background="#efefef" Margin="8,0,2,0" Width="470" HorizontalAlignment="Stretch" Hint=" search" Foreground="Black" TextChanged="enterNameTxt_TextChanged" BorderThickness="8,3,3,3" Grid.ColumnSpan="2"/>

                        <GkJumpList:JumpList x:Name="contactsListBox" Grid.Row="1" Tap="contactSelected_Click" Margin="25,0,0,0" Grid.ColumnSpan="2" d:LayoutOverrides="VerticalMargin" ShowRowHeader="False" ShowHeaders="True" IsPumped="False" PicturePath="{Binding Msisdn,Converter={StaticResource imgConverter}}">
                            <GkJumpList:JumpList.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="3,3,0,3">
                                        <Image x:Name="avatar_image" Height="55" HorizontalAlignment="Left" VerticalAlignment="Center" 
                               Source="{Binding Msisdn, Converter={StaticResource imgConverter}}"/>
                                        <StackPanel Orientation="Vertical">
                                            <StackPanel x:Name="Sender" Height="Auto"  Orientation="Vertical" HorizontalAlignment="Left" Width="Auto" Margin="20,2,0,2">
                                                <TextBlock Text="{Binding Name}" Foreground="Black" FontSize="36" />
                                                <StackPanel Orientation="Horizontal">
                                                    <Image Source="{Binding OnHike, Converter={StaticResource onHikeConverter}}" Height="15" Width="15"/>
                                                    <TextBlock Text="{Binding Msisdn}" Foreground="Gray" FontSize="20" Height="30" Margin="6,0,0,0" />
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </GkJumpList:JumpList.ItemTemplate>
                        </GkJumpList:JumpList>
                    </Grid>
                    <StackPanel x:Name="sendMsgPanel" Orientation="Horizontal" Margin="-12,580,0,0" Grid.Row="2">
                        <TextBox x:Name="enterMsgTxt" Width="442" TextChanged="enterNameTxt_TextChanged" />
                    </StackPanel>
                </Grid>

            </controls:PivotItem>-->


            <controls:PivotItem Header="profile">
                <Grid x:Name="ProfileGrid">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30, 10, 0,0">
                        <Button Height="Auto" Click="onProfilePicButtonClick">
                            <StackPanel Orientation="Horizontal" Margin="-15">
                                <Border BorderBrush="Black" BorderThickness="3">
                                    <Image x:Name="avatarImage" Height="80"  Source="images\ic_avatar0.png" />
                                </Border>
                            </StackPanel>
                        </Button>

                        <StackPanel Orientation="Vertical" Margin="20, 0,0,0">
                            <TextBlock x:Name="accountName"  Foreground="Black" FontSize="35" Text="UNKNOWN"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Free SMS remaining: " Foreground="#8d8d8d"></TextBlock>
                                <TextBlock x:Name="creditsTxtBlck" Text="" Foreground="#8d8d8d"></TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="30, 30, 0,10" Orientation="Vertical" Grid.Row="1">
                        <StackPanel Orientation="Horizontal" MaxHeight="55" Margin="0,15,0,5">
                            <Image Source="images\purchases.png" Height="35"></Image>
                            <TextBlock Text="Purchases" Margin="15,4,0,0" FontSize="30"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" MaxHeight="55" Margin="0,15,0,5">
                            <Image Source="images\notifications.png" Height="35"></Image>
                            <TextBlock Text="Notifications" Margin="15,4,0,0" FontSize="30"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" MaxHeight="55" Margin="0,15,0,5">
                            <Image Source="images\privacy.png" Height="35"></Image>
                            <TextBlock Text="Privacy" Margin="25,4,0,0" FontSize="30"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" MaxHeight="55" Margin="0,15,0,5">
                            <Image Source="images\help.png" Height="35"></Image>
                            <TextBlock Text="Help" Margin="15,4,0,0" FontSize="30"></TextBlock>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Row="2">
                        <Image Source="images\Made_with_love.png" Height="20" Margin="0,0,0,10"/>

                    </StackPanel>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton x:Name="appbar_button1" IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton x:Name="appbar_button2" IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem x:Name="menuItem1" Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem x:Name="menuItem2" Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>