<phone:PhoneApplicationPage 
    x:Class="windows_client.View.ChatThread"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:c4fToolkit="clr-namespace:Coding4Fun.Phone.Controls;assembly=Coding4Fun.Phone.Controls"
    xmlns:Converters="clr-namespace:windows_client.converters"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True" Visibility="Visible">

    <phone:PhoneApplicationPage.Resources>
        <Style x:Key="ListBoxItemStretchContentStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.Resources>
            <Converters:ChatThreadAlignment x:Key="alignmentConverter"></Converters:ChatThreadAlignment>
            <Converters:ChatBubbleDirection x:Key="bubbleDirectionConverter"/>
            <Converters:ChatThreadBackground x:Key="bubbleBackgroundConverter"/>
            <Converters:ChatThreadSDRImageShow x:Key="sdrVisibilityConverter"/>
            <Converters:ChatThreadSDRImage x:Key="sdrImageConverter"/>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="HikeTitle" Grid.Row="0" Margin="12,17,0,28" Background="AliceBlue">
            <TextBlock x:Name="hikeLabel" Text="" Foreground="#FF1A1919"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <ListBox x:Name="myListBox" Grid.Row="1" Grid.ColumnSpan="2" ItemContainerStyle="{StaticResource ListBoxItemStretchContentStyle}" FontSize="26" Margin="0,0,0,75">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!--<edc:Callout VerticalAlignment="Stretch" HorizontalAlignment="Right" AnchorPoint="0,1.5" CalloutStyle="RoundedRectangle" Content="{Binding Message}" Fill="DeepSkyBlue" FontSize="25" Stroke="Black" MinWidth="100" MinHeight="60" Margin="10, 5, 5, 10" Height="Auto" Width="Auto" MaxWidth="400"/>-->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="{Binding IsSent, Converter={StaticResource alignmentConverter}}">
                        
                        <Image Visibility="{Binding MessageStatus, Converter={StaticResource sdrVisibilityConverter}}" Source="{Binding MessageStatus, Converter={StaticResource sdrImageConverter}}" HorizontalAlignment="Right"></Image>
                        <c4fToolkit:ChatBubble Background="{Binding IsSent, Converter={StaticResource bubbleBackgroundConverter}}" MinWidth="50" Margin="25,10,25,10" VerticalAlignment="Stretch" 
                                           MaxWidth="350" HorizontalAlignment="{Binding IsSent, Converter={StaticResource alignmentConverter}}" 
                                           FontFamily="Arial" ChatBubbleDirection="{Binding IsSent, Converter={StaticResource bubbleDirectionConverter}}">
                            <TextBlock TextWrapping="Wrap" Foreground="Black" Text="{Binding Message}"/>
                            <toolkit:ContextMenuService.ContextMenu>
                                <toolkit:ContextMenu Name="ctxMenu">
                                    <toolkit:MenuItem Header="Copy"  Click="MenuItem_Click_Copy"/>
                                    <toolkit:MenuItem Header="Forward"  Click="MenuItem_Click_Forward"/>
                                    <toolkit:MenuItem Header="Delete"  Click="MenuItem_Click_Delete"/>
                                </toolkit:ContextMenu>
                            </toolkit:ContextMenuService.ContextMenu>
                        </c4fToolkit:ChatBubble>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--<StackPanel Orientation="Horizontal" Margin="100,0,0,0">
            <Image Source="images\ic_read_small.png" HorizontalAlignment="Right"></Image>
            <c4fToolkit:ChatBubble Background="Wheat" MinWidth="50" Margin="25,10,25,10" VerticalAlignment="Stretch" 
                                           MaxWidth="350" HorizontalAlignment="Right" 
                                           FontFamily="Arial" ChatBubbleDirection="LowerRight">
                <TextBlock TextWrapping="Wrap" Foreground="Black" Text="hellos"/>
                <toolkit:ContextMenuService.ContextMenu>
                    <toolkit:ContextMenu Name="ctxMenu">
                        <toolkit:MenuItem Header="Copy"  Click="MenuItem_Click_Copy"/>
                        <toolkit:MenuItem Header="Forward"  Click="MenuItem_Click_Forward"/>
                        <toolkit:MenuItem Header="Delete"  Click="MenuItem_Click_Delete"/>
                    </toolkit:ContextMenu>
                </toolkit:ContextMenuService.ContextMenu>
            </c4fToolkit:ChatBubble>
        </StackPanel>-->
        <StackPanel x:Name="sendMsgPanel" Grid.Row="1" Margin="0,0,0,0" Orientation="Horizontal" VerticalAlignment="Bottom" Background="WhiteSmoke">
            <toolkit:PhoneTextBox BorderThickness=".5" BorderBrush="Gray" x:Name="sendMsgTxtbox" Hint="Free Message..."  Width="369" TextWrapping="Wrap" Background="White" TextChanged="sendMsgTxtbox_TextChanged" />
            <Button BorderThickness="0" x:Name="sendMsgBtn" Height="75" Width="Auto" Content="Send" Click="sendMsgBtn_Click">
                <Button.Background>
                    <ImageBrush ImageSource="Images\ic_bottom_button.png" Stretch="Fill"/>
                </Button.Background>
            </Button>
        </StackPanel>
    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text = "Block User" Click="blockUnblock_Click"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>
