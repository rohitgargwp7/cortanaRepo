<phone:PhoneApplicationPage 
    x:Class="windows_client.View.GroupInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="White">
        <!--Pivot Control-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="HikeTitle" Orientation="Vertical" Margin="0,0,0,0" Height="Auto">
            <Image Source="images\hike_logo_small.png" Margin="25,20,0,0" HorizontalAlignment="Left" Height="34" Width="127"></Image>
            <TextBlock Text="group info" FontSize="60" FontStretch="Expanded"  Margin="23,0,0,0" Foreground="Black" Height="80"/>
        </StackPanel>

        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>


            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30, 10, 0,0" Height="100">
                <Button Height="Auto">
                    <StackPanel Orientation="Horizontal" Margin="-15">
                        <Border BorderBrush="Black" BorderThickness="3">
                            <Image x:Name="groupImage" Height="80" Width="80" Tap="onGroupProfileTap" Source="images\ic_avatar0.png" />
                        </Border>
                    </StackPanel>
                </Button>
                <StackPanel Orientation="Vertical" Margin="20, 0,0,0">
                    <TextBlock Foreground="Black" FontSize="25" Text="Group Name"/>
                    <TextBlock x:Name="groupName" Foreground="Black" FontSize="35"></TextBlock>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="35,30,0,0">
                <Image Source="images\add_users.png" Tap="AddParticipants_Tap" Width="30" Height="30"></Image>
                <TextBlock Text="Participants" Foreground="Black" FontSize="35" Margin="20,0,0,0"></TextBlock>
            </StackPanel>
            <ListBox x:Name="groupChatParticipants" Grid.Row="2" Margin="35,5,0,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Height="25" Width="25" Fill="#1ba1e2"></Rectangle>
                            <TextBlock Foreground="Black" Text="{Binding Name}" FontSize="30" Margin="20,0,0,0"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Grid.Row="3">
                <Button Content="Invite SMS participants to hike" Tap="inviteSMSUsers_Tap" Height="90" FontSize="20" BorderThickness="2" BorderBrush="Black" Width="380" Foreground="Black"></Button>
            </StackPanel>
            
        </Grid>
    </Grid>
</phone:PhoneApplicationPage>
