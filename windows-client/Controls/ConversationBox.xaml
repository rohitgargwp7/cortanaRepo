<UserControl x:Class="windows_client.Controls.ConversationBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:windows_client.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <Image x:Name="profileImage" Height="83" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="1"></Image>
        <TextBlock x:Name="userName" Grid.Column="1" Grid.Row="0"></TextBlock>
        <!--<local:LinkifiedTextBox Width="340" Grid.Row="0" Grid.Column="1" Text="{Binding Text, Mode=OneTime}" x:Name="MessageText" Margin="0,6,0,0" />-->
        <TextBlock x:Name="lastMessage" Grid.Column="1" Grid.Row="1"/>
        <TextBlock x:Name="timestamp" Grid.Column="2" Grid.Row="0"></TextBlock>

    </Grid>
</UserControl>
